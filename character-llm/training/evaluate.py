"""Evaluate the fine-tuned character LLM."""


def evaluate(model_path: str = "../models/character-model") -> None:
    print(f"Evaluating model at {model_path}... (placeholder)")


if __name__ == "__main__":
    evaluate()
